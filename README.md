# ROCS
Roblox Component System

--[[
		Lifecycle methods
		- onAdded(entity, reducedComponent)
        - onRemoved(entity, ...)
        - onHeartbeat(number dt)
        - initialize()
        - destroy()
]]
